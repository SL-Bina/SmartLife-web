/**
 * Legacy DynamicToast — now a no-op.
 * All toast rendering is handled by <DynamicToastContainer />.
 * This file is kept so existing imports don't break.
 */
export default function DynamicToast() {
  return null;
}
